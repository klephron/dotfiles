return function()
  local dressing = require("dressing")
  dressing.setup({
    input = {
      insert_only = false,
      win_options = {
        winblend = 5,
      },
      mappings = {},
    },
  })

end
